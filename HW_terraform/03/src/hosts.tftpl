[webservers]

%{~ for i in webservers ~}

VM_name=${i["name"]}   ansible_host=${i["network_interface"][0]["nat_ip_address"]}     FQDN=${i["fqdn"]}

%{~ endfor ~}

[vm]

%{~ for i in vm ~}

VM_name=${i["name"]}   ansible_host=${i["network_interface"][0]["nat_ip_address"]}     FQDN=${i["fqdn"]}

%{~ endfor ~}

[storage]

%{~ for i in storage ~}

VM_name=${i["name"]}   ansible_host=${i["network_interface"][0]["nat_ip_address"]}     FQDN=${i["fqdn"]}

%{~ endfor ~}
